import 'package:flutter/material.dart';

class Lugares extends StatelessWidget {
  const Lugares({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Mira los lugares'),
    );
  }
}
